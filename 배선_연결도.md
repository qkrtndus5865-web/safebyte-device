# Safe-Byte 하드웨어 배선 연결도

카트 거치형 OCR 모듈의 전체 배선 정리.
연결은 **① 전원선(힘) · ② 신호선(데이터) · ③ 공통 GND(기준선)** 세 종류로 나눠서 본다.

> ⚠️ 확정 필요한 값
> - 네오픽셀 개수 → 코드 `NUM_PIXELS` 값에 반영
> - 7인치 LCD 모델명 → 전원·터치 연결 방식이 모델마다 다름

---

## 부품 목록

- PD 보조배터리 (USB 포트 2개: USB-C, USB-A)
- 라즈베리파이 5
- 아두이노 나노
- 네오픽셀 링 (WS2812B)
- 방수 푸시버튼
- 액티브 부저
- Pi 카메라 모듈
- 7인치 터치 LCD

---

## ① 전원선 (누가 전기를 받나)

| 받는 쪽 | 주는 쪽 | 연결 방법 |
|---|---|---|
| 라즈베리파이 5 | 보조배터리 **USB-C** 포트 | USB-C 케이블 |
| 아두이노 나노 | **라즈베리파이의 USB-A** 포트 | USB 케이블 (전원+시리얼 동시) |
| 네오픽셀 5V | 보조배터리 **USB-A** 포트 → USB 분배보드 | 분배보드 5V 단자 → 점프선 |

**핵심:** 나노는 배터리가 아니라 Pi의 USB-A에 꽂힌다(전원+통신 동시).
그래서 배터리의 남는 USB-A 포트가 네오픽셀 몫이 된다.

---

## ② 신호선·데이터선 (명령이 오가는 선)

### 아두이노 나노 핀

| 나노 핀 | 연결 부품 | 방향 | 연결 방법 |
|---|---|---|---|
| **D6** | 네오픽셀 **DIN** | 나노 → LED (데이터) | 점프선 |
| **D7** | 방수 버튼 한쪽 다리 | 버튼 → 나노 (입력) | 점프선 |
| **D8** | 액티브 부저 **+**(긴 다리) | 나노 → 부저 (출력) | 점프선 |
| **USB** | Pi USB-A | 시리얼 양방향 | USB 케이블 |

### 라즈베리파이 5 커넥터

| Pi 커넥터 | 연결 부품 | 연결 방법 |
|---|---|---|
| **CSI** 포트 | 카메라 모듈 | 리본 케이블 (Pi5는 22핀) |
| **DSI** 포트 | 7인치 LCD | 리본 케이블 |
| USB-A | 아두이노 나노 | USB 케이블 |
| USB-C | 배터리 | 전원 |

> **리본 케이블(ribbon cable)** = 여러 가닥 선이 납작하게 나란히 붙은 띠 모양 케이블.
> 라즈베리파이 5는 CSI·DSI 커넥터가 **22핀(0.5mm)** → 파이 4용 15핀 리본과 안 맞음. **Pi 5용 리본 필요.**

---

## ③ 공통 GND (가장 중요 — 다 같이 묶임)

GND(접지, 0V 기준선)는 아래 전부를 한 점에 묶어야 한다.

```
공통 GND 버스 ─┬─ 보조배터리 GND (USB-A의 검정선)
               ├─ 네오픽셀 GND
               ├─ 나노 GND
               ├─ 방수 버튼 다른쪽 다리
               └─ 액티브 부저 −(짧은 다리)
```

**이유:** 데이터 신호(D6·D7·D8)는 GND를 0V 기준으로 삼아 "높다/낮다"를 판단한다.
부품마다 GND가 다르면 기준이 어긋나 신호가 깨진다. → 모든 GND를 하나로.

---

## 부품별 연결점 전부

### 네오픽셀 링 (연결점 3개)
- DIN → 나노 D6
- 5V → 배터리 5V (USB 분배보드)
- GND → 공통 GND

### 방수 푸시버튼 (연결점 2개)
- 한쪽 다리 → 나노 D7
- 다른쪽 다리 → 공통 GND
- 코드가 `INPUT_PULLUP`이라 **저항 추가 불필요** (안 누르면 HIGH, 누르면 GND에 닿아 LOW)

### 액티브 부저 (연결점 2개, 극성 있음)
- +(긴 다리) → 나노 D8
- −(짧은 다리) → 공통 GND
- **극성 주의:** 거꾸로 꽂으면 소리 안 남. 긴 다리가 +.

### 카메라 모듈 (연결점 1개)
- CSI 리본 → Pi CSI 포트 (리본 방향/파란 띠 위치 주의)

### 7인치 터치 LCD (모델에 따라 다름 ⚠️)
- 화면: DSI 리본 → Pi DSI 포트
- 전원·터치: **모델마다 다름.** 공식 DSI LCD는 Pi에서 리본으로 함께 받기도 하고,
  어떤 제품은 별도 5V 전원선 + 터치용 USB선이 따로 필요. → 모델명 확인 후 확정.

---

## 전체 연결 그림

```
[PD 보조배터리]
  ├─USB-C──────────────→ [라즈베리파이 5]
  │                          ├─USB-A──→ [아두이노 나노] (전원+시리얼)
  │                          ├─CSI 리본─→ [카메라 모듈]
  │                          └─DSI 리본─→ [7인치 LCD]
  │
  └─USB-A──→[USB 분배보드]
               ├─5V ──→ 네오픽셀 5V
               └─GND ─┐
                      │
[아두이노 나노]        │
  ├─D6 ──점프선──→ 네오픽셀 DIN
  ├─D7 ──점프선──→ 방수버튼 ──┐
  ├─D8 ──점프선──→ 부저(+) ──┐│
  └─GND ─────────────────────┼┼─── 공통 GND ───┐
                             ││                 │
        방수버튼 다른쪽 ──────┘│                 │
        부저(−) ──────────────┘                 │
        네오픽셀 GND ───────────────────────────┤
        배터리 GND ─────────────────────────────┘
```

---

## 연결 방법 요약 (무엇으로 잇나)

| 연결 | 방법 |
|---|---|
| 배터리↔Pi, Pi↔나노 | USB 케이블 (꽂기만) |
| 카메라·LCD↔Pi | 리본 케이블 (꽂기만, Pi5용 22핀) |
| 배터리↔네오픽셀 5V/GND | USB 분배보드 + 나사단자/점프선 (납땜 최소화) |
| 나노↔버튼/부저/네오픽셀 DIN | 점프선, 필요시 납땜(꼬기+수축튜브) |

---

## 전원 온/오프 순서 (참고)

microSD 손상 방지를 위해 **소프트웨어 종료 → 전원 차단** 순서를 지킬 것.

**켤 때**
1. 보조배터리 출력 ON (또는 마스터 스위치 ON)
2. 라즈베리파이 5 자동 부팅 (또는 Pi 5 내장 전원 버튼 짧게 눌러 부팅)

**끌 때**
1. 라즈베리파이 5 내장 전원 버튼 짧게 누르기 → 안전 종료 (약 10초 대기)
2. Pi 완전히 꺼진 뒤 → 보조배터리 출력 OFF

> 보조배터리 함정: 소비 전류가 적으면 자동으로 출력을 끄는 제품이 있음.
> → "저전류 모드(trickle/low-current)" 또는 상시 출력 기능 있는 배터리 권장.
